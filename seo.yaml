enabled: true
active: true

# Adds json_ld for all data set (can be disabled on a per schema basis)
add_json_ld: true

faq_page:
  table_of_contents: true
  table_of_contents_title_level: h2
  title_level: h3
  # add_json_ld: false

aggregate_rating:
  # add_json_ld: false

local_business:
  # add_json_ld: false

  # Only works if geocoding plugin is installed
  resolve_geolocation: false
