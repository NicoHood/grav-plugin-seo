title: Faq Page
extends@:
    type: default
    context: blueprints://pages

form:
  fields:
    tabs:
      fields:
        seo:
          fields:
            tabs:
              fields:
                # NOTE: Disable faq_page tab in seo plugin
                faq_page:
                  unset@: true

        content:
          import@:
            type: 'partials/structured-data/faq_page'
            context: blueprints://
